export const enum Routes {
    SignIn = "/sign-in",
    SignUp = "/sign-up",
    Main = "/main",
    Editor = "/editor",
    Fallback = "/*",
    Root = "/",
}

export const enum ReqState {
    Pending = "Pending",
    Idle = "Idle",
    Failed = "Failed",
}
